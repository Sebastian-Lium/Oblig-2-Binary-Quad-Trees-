#include <iostream>
#include "binarynode.h"
/*
int main(int argc, char *argv[])
{
    BinaryNode* h = new BinaryNode('d');
    BinaryNode* v = new BinaryNode('a');
    v = new BinaryNode('b', v, h);
    // peker h er ledig
    BinaryNode* btre = new BinaryNode('g');
    h = new BinaryNode('p');
    btre = new BinaryNode('i', btre, h);
    btre = new BinaryNode('f', v, btre);
    
    std::cout << "Recursive: ";
    btre->intrav();
    std::cout << "\nIterative: ";
    btre->inOrder(btre);
    std::cout << "\nPost Order: ";
    btre->postOrder(btre);
    std::cout << "\nTotal number of nodes: " << btre->getFullCount(btre);
    std::cout << "\nDepth: " << btre->maxDepth(btre);
    std::cout << "\nIs tree balanced? ";
    bool(btre->isBalanced(btre)) ? std::cout << "true" : std::cout << "false";
    std::cout << std::endl;
}*/
